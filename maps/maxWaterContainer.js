/**
 * @param {number[]} height
 * @return {number}
 */
var maxArea = function (height) {
  /**
   ** Based on the problem it seems that DP is going to be needed, a hash table is needed
   * for storing the possible combinations and calculate them at the end of the problem.
   * //This sounds trivial.
   * 
   * 
   * Brute force approach.
   * -have a variable to store the maximum area.
   * -
   *
   *
   */
};
